{% layout 'theme' %}

<div class="page-width">
  <div class="gr-skaters-page">
    <header class="gr-skaters-header">
      <h1>{{ page.title }}</h1>
      {% if page.content != blank %}
        <div class="gr-skaters-description">
          {{ page.content }}
        </div>
      {% endif %}
    </header>
    
    {% section 'gr-skaters-catalog' %}
  </div>
</div>

<style>
  .gr-skaters-page {
    margin: 40px 0;
  }
  
  .gr-skaters-header {
    text-align: center;
    margin-bottom: 30px;
  }
  
  .gr-skaters-header h1 {
    margin-bottom: 15px;
    color: {{ settings.gr_color_principal }};
  }
  
  .gr-skaters-description {
    max-width: 800px;
    margin: 0 auto;
  }
</style>
