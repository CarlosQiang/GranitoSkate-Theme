{% layout 'theme' %}

<div class="page-width">
  <div class="gr-spots-page">
    <header class="gr-spots-header">
      <h1>{{ page.title }}</h1>
      {% if page.content != blank %}
        <div class="gr-spots-description">
          {{ page.content }}
        </div>
      {% endif %}
    </header>
    
    {% section 'gr-spots-catalog' %}
  </div>
</div>

<style>
  .gr-spots-page {
    margin: 40px 0;
  }
  
  .gr-spots-header {
    text-align: center;
    margin-bottom: 30px;
  }
  
  .gr-spots-header h1 {
    margin-bottom: 15px;
    color: {{ settings.gr_color_principal }};
  }
  
  .gr-spots-description {
    max-width: 800px;
    margin: 0 auto;
  }
</style>
