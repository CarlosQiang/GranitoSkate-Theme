&lt;!DOCTYPE html>
<html>
<head>
  <title>{{ product.title }} | {{ shop.name }}</title>
  {{ 'gr-styles.css' | asset_url | stylesheet_tag }}
  {{ content_for_header }}
</head>
<body>
  {% section 'gr-header' %}
  
  {% section 'gr-product-template' %}
  
  <div id="product-reviews" data-product-id="{{ product.id }}"></div>
  
  {% section 'gr-product-recommendations' %}
  
  {% section 'gr-footer' %}
  
  {{ content_for_footer }}
</body>
</html>
