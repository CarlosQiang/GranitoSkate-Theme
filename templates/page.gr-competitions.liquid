{% layout 'theme' %}

<div class="page-width">
  <div class="gr-competitions-page">
    <header class="gr-competitions-header">
      <h1>{{ page.title }}</h1>
      {% if page.content != blank %}
        <div class="gr-competitions-description">
          {{ page.content }}
        </div>
      {% endif %}
    </header>
    
    {% section 'gr-competitions' %}
  </div>
</div>

<style>
  .gr-competitions-page {
    margin: 40px 0;
  }
  
  .gr-competitions-header {
    text-align: center;
    margin-bottom: 30px;
  }
  
  .gr-competitions-header h1 {
    margin-bottom: 15px;
    color: {{ settings.gr_color_principal }};
  }
  
  .gr-competitions-description {
    max-width: 800px;
    margin: 0 auto;
  }
</style>
